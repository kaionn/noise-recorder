name: NoiseRecorder
options:
  bundleIdPrefix: com.kaionn
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "6.0"

targets:
  NoiseRecorder:
    type: application
    platform: iOS
    sources:
      - NoiseRecorder
    settings:
      base:
        INFOPLIST_FILE: NoiseRecorder/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.kaionn.noise-recorder
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        DEVELOPMENT_TEAM: ""
